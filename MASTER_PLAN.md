# Архитектурный План Разработки: AutoFL — автономный фриланс-агент

> Детализированная версия плана. Статус каждого этапа отслеживается в `progress.json`.
> Домен, таксономия заданий и матрица рисков — в `docs/SCOPE.md` (Этап 0).

## Обзор архитектуры

```
            ┌─────────────────────────────────────────────────────────┐
            │ discovery/  fetcher → dedupe → Candidate               │
            │      │                                                  │
            │      ▼                                                  │
            │ brain/  safety (правила) → classifier (LLM) → verdict  │
            │      │                                                  │
            │      ▼                                                  │
            │ workflow/  state_machine → approvals (human-in-the-loop)│
            │      │                                                  │
            │      ▼                                                  │
            │ executor/  text · data · code(sandbox) → quality → OK  │
            │      │                                                  │
            │      ▼                                                  │
            │ platforms/  base adapter → kwork · flru · youdo        │
            │      │                     (Playwright, без API)        │
            │      ▼                                                  │
            │ comms/  messages (заказчик) · telegram_admin (оператор) │
            └─────────────────────────────────────────────────────────┘
            db/ (SQLAlchemy + Alembic) · observability/ · api/ (FastAPI)
```

- **Вход**: ленты заданий фриланс-площадок (Playwright; у большинства площадок нет публичного API).
- **Ядро**: детерминированная безопасность + LLM-классификация + машина состояний + гейты одобрения.
- **LLM**: pluggable-провайдер (локальный Ollama по умолчанию / внешний API), структурный вывод через pydantic.
- **Выход**: отправка результата, переписка с заказчиком, алерты оператору в Telegram.

## Этап 0: Скоуп, таксономия заданий и анализ рисков

**Цель**: зафиксировать границы системы — что берём в работу, что никогда, и почему.

1. `docs/SCOPE.md`:
   - целевые площадки, уровень автономии, юридический дисклеймер;
   - таксономия исполняемых заданий (текст / данные / код в песочнице);
   - стоп-лист (регистрации, отзывы на маркетплейсах, «заработай», карты/паспорта, чужие аккаунты);
   - матрица рисков (бан, капча, скам, качество, вывод денег) с митигациями;
   - фазы развёртывания (A read-only → B draft-only → C supervised → D semi-auto).
2. Зафиксировать в `progress.json` этапы 0–11.
3. Политика «ничего не отправляется без одобрения» до завершения Этапа 8.

**Критерий готовности**: `docs/SCOPE.md` заполнен и согласован; все этапы в `progress.json`.

## Этап 1: Каркас проекта и конфигурация

**Цель**: рабочий репозиторий, конфиг через pydantic-settings, логирование.

1. `.gitignore` (исключены `.env*`, `venv/`, `data/`, `logs/`, БД, сессии браузера).
2. `README.md` (описание, архитектура, быстрый старт, таблица конфигов).
3. `requirements.txt` (секции, пиннинг): fastapi/uvicorn/pydantic(-settings)/httpx,
   sqlalchemy/alembic/aiosqlite, playwright, aiogram, pytest/pytest-asyncio.
4. `pyproject.toml` — конфиг pytest (`asyncio_mode=auto`, `testpaths`).
5. `src/config/settings.py` — pydantic-settings; `src/observability.py` — rotating-логи;
   `src/main.py` — точка входа; `.env.example` — все параметры с комментариями.
6. Каркас пакетов `src/` и `tests/` (импортируемые модули).

**Критерий готовности**: `python -m compileall src tests` без ошибок;
`python -m src.main` логирует конфигурацию; `progress.json` обновлён.

## Этап 2: Слой данных

**Цель**: долгоживущее состояние системы — аккаунты, задания, отправки, заработок.

1. `src/db/models.py` (SQLAlchemy 2.0, async): `Platform`, `Account`, `Session`
   (сохранённая браузерная сессия), `Candidate` (найденное задание), `Task`
   (принятое в работу), `Submission`, `Revision`, `Earning`, `AuditLog`.
2. `src/db/session.py` — async engine + фабрика сессий; `src/db/init_db.py` — create_all.
3. `src/db/migrations/` — Alembic: `alembic init`, первый авто-рендер схемы.
4. Индексы и ограничения уникальности: `(platform, external_id)` для заданий.

**Критерий готовности**: `alembic upgrade head` проходит; unit-тест CRUD
(`tests/test_db.py`) зелёный; конфиг `DB_URL` читается из `.env`.

## Этап 3: Абстракция платформ и авторизация

**Цель**: единый интерфейс для всех площадок; сессии переживают перезапуски.

1. `src/platforms/base.py` — `PlatformAdapter` (ABC): `discover()`, `apply()`,
   `submit()`, `send_message()`; датаклассы `TaskCandidate`, `SubmissionResult`.
2. `src/platforms/auth.py` — Playwright-контур: ручной вход (оператор), сохранение
   `storage_state`, проверка живости сессии, прокси, fingerprint, обработка капчи
   (только вручную → алерт оператору).
3. Адаптеры по одному за шаг: `kwork.py`, `flru.py`, `youdo.py`.
4. `src/platforms/registry.py` — фабрика адаптеров по `PLATFORMS` из `.env`.

**Критерий готовности**: на записанных HTML-снапшотах адаптер извлекает задания
и логинится в dry-режиме без живых площадок.

## Этап 4: Поиск и дедупликация заданий

**Цель**: стабильный, вежливый сбор ленты заданий.

1. `src/discovery/fetcher.py` — пагинация, фильтры (категория/бюджет/срок),
   rate-limit + рандомизированные задержки, обработка вылета сессии.
2. `src/discovery/dedupe.py` — уникальность по `(platform, external_id)`,
   change-detection (изменение бюджета/сроков), сохранение `Candidate`.

**Критерий готовности**: из снапшота ленты извлекаются все задания;
повторный прогон не создаёт дубликатов (тест `tests/test_dedupe.py`).

## Этап 5: Анализ и безопасность задач (ядро)

**Цель**: отличать «простое безопасное задание» от скама и нарушений ToS.

1. `src/brain/safety.py` — **детерминированные правила ДО LLM**: стоп-словарь
   (регистрации, накрутка, отзывы на маркетплейсах, карты/паспорта, переводы,
   капча, «перейди по ссылке»), скоринг.
2. `src/brain/classifier.py` — LLM со структурным выводом (pydantic):
   `{category, complexity, doable, safety_score, reasons, confidence}`.
3. `src/brain/prompts.py` — версионируемые системные промпты.
4. Порог допуска: `MIN_SAFETY_SCORE` (по умолчанию 0.85); ниже → отклонено.

**Критерий готовности**: `tests/test_safety.py` (правила) и `tests/test_classifier.py`
(мок LLM) зелёные; на размеченном датасете отсев скама ≥ 95% (Этап 10 — метрика).

## Этап 6: Движок выполнения

**Цель**: реальное производство результата для простых заданий.

1. `src/executor/base.py` — `Executor` (ABC): `async run(task) -> Deliverable`.
2. `src/executor/text.py` — rewrite, перевод, описание товара, отзыв, SEO-текст.
3. `src/executor/data.py` — парсинг, преобразование форматов, таблицы, дедуп данных.
4. `src/executor/code.py` — только в песочнице `src/sandbox/runner.py`
   (Docker/subprocess: без сети, лимиты времени/памяти, таймаут).
5. `src/executor/quality.py` — самопроверка результата (фактология, длина, соответствие ТЗ),
   ограниченная регенерация (bounded), иначе — эскалация оператору.

**Критерий готовности**: unit-тесты исполнителей; код из задания выполняется
изолированно и не имеет сетевого доступа.

## Этап 7: Рабочий процесс и коммуникация

**Цель**: надёжный конвейер без дублей и потерь задач.

1. `src/workflow/state_machine.py` — статусы и допустимые переходы
   (`discovered → classified → rejected|queued → approved → applied →
   executing → quality_check → ready → submitted → delivered →
   revision_requested|done|cancelled → paid`); переходы атомарны, идемпотентны.
2. `src/workflow/pipeline.py` — оркестратор цикла
   discover → classify → approve → execute → submit.
3. `src/comms/messages.py` — переписка с заказчиком, ограниченные ревизии
   (`MAX_REVISIONS`, дальше — эскалация).

**Критерий готовности**: `tests/test_state_machine.py` покрывает все переходы
и восстановление после «краша» на любом шаге (идемпотентность).

## Этап 8: Human-in-the-loop, безопасность, лимиты

**Цель**: никакое внешнее действие не выполняется без контроля, пока не включена
автономность с жёсткими лимитами.

1. `src/workflow/approvals.py` — гейты одобрения. По умолчанию
   `APPROVAL_REQUIRED=true`: отклик/отправка/сообщение → запрос в админ-бот.
2. `src/comms/telegram_admin.py` — админ-бот (aiogram): сводки, кнопки
   approve/reject, алерты об аномалиях, kill switch.
3. Лимиты из конфига: `MAX_CONCURRENT_TASKS`, `DAILY_BUDGET_RUB`,
   `MIN_SAFETY_SCORE`, `DRY_RUN`, `AUTO_EXECUTE`.
4. Редакция секретов в промптах и логах (карты, пароли, токены).

**Критерий готовности**: dry-run и kill switch работают; действие выше порога
не проходит без аппрува; `tests/test_approvals.py` зелёный.

## Этап 9: Наблюдаемость и эксплуатация

**Цель**: оператор всегда видит состояние системы.

1. `src/observability.py` — расширение: структурированные логи (rotating),
   метрики (найдено/выполнено/заработок/ошибки), аудит-лог в БД.
2. `src/api/app.py` — FastAPI: `/healthz`, `/status`, `/tasks`, `/approve`, `/earnings`.
3. `docs/RUNBOOK.md` — восстановление сессий, действия при бане/капче.

**Критерий готовности**: `/healthz` → 200; метрики и алерты видны в админ-боте.

## Этап 10: Тестирование и CI

**Цель**: устойчивость к изменениям площадок и регрессии логики.

1. Unit: `classifier`, `safety`, `dedupe`, `state_machine`, `executor`, `quality`
   (LLM — мок; снапшоты HTML — записанные фикстуры, без живых площадок).
2. Метрика классификатора: отсев скама ≥ 95%, ложных отклонений ≤ 10%.
3. E2E в dry-run: discover → classify → execute → (mock submit).
4. `pytest` в `pyproject.toml`; прогон в CI (GitHub Actions) — опционально.

**Критерий готовности**: `pytest -v` полностью зелёный.

## Этап 11: Поэтапное развёртывание

**Цель**: включение автономности без риска мгновенного бана.

1. **Фаза A (read-only)**: только поиск + классификация + отчёт в Telegram.
   Никаких мутаций на площадках.
2. **Фаза B (draft-only)**: LLM готовит черновики; отправляет оператор вручную.
3. **Фаза C (supervised)**: авто-отправка только низкорисковых задач
   (score ≥ порога), с аппрувами и дневными лимитами.
4. **Фаза D (semi-auto)**: автономный цикл с лимитами, kill switch,
   ежедневной ревизией оператором.
5. **Вывод денег — всегда вручную**, вне скоупа бота (KYC/налоги).

**Критерий готовности**: каждая фаза стабильна ≥ 1 недели без инцидентов
и бана аккаунта.

---

## Трек B: Автономное создание аккаунтов и финансы

> Отдельный трек, дополняющий трек A (поиск/выполнение). Оператор командует
> «заведи учётные записи» — система сама регистрирует аккаунты и распоряжается
> средствами (карта/СБП) как ресурсом: тратит на SMS-активацию, капчу, почту,
> подписки и принимает оплату заказов напрямую на карту.

> **Статус**: каркас и dry-run-провайдеры (почта/SMS/капча) реализованы —
> конвейер регистрации работает и тестируется без внешних сервисов
> (B2–B5, B7). Реальные провайдеры подключаются в `src/provisioning/providers.py`
> по мере получения API-ключей (`PROVISIONING_PROVIDER`).

### Архитектура трека B

```
agent/        commander (NL-команды) → planner (декомпозиция → jobs)
                 │
                 ▼
provisioning/ identity → email → phone(SMS-активация) → captcha
                 │        → registrar (Playwright) → vault (учётки)
                 ▼
finance/      requisites (карта/СБП) → payment_verification (приход)
              wallet (ledger) · budget (лимиты) · spending (авто-траты)
                 │
                 ▼
comms/        negotiation (заключение заказа, настаивание на карту)
```

### B1 — Карта и платёжная инфраструктура ✅ done

1. `src/security/vault.py` — шифрование (Fernet): карты, пароли, сессии;
   ключ из `VAULT_KEY` либо автогенерация в `data/vault.key` (gitignored).
2. `src/finance/requisites.py` — сборка реквизитов для заказчика
   (СБП-номер или карта, ФИО держателя, банк; PAN только зашифрован).
3. `src/finance/payment_verification.py` — подтверждение прихода:
   режимы `manual` (оператор) и `bank_sms` (сверка суммы).
4. `src/finance/wallet.py`, `sources.py`, `budget.py`, `spending.py` —
   ledger доходов/расходов, источники, лимиты категорий, авто-порог трат.
5. БД: `FundingSource` (card/sbp), `Transaction` (income/expense,
   категории); правки `docs/SCOPE.md` (прямая оплата на карту, 115-ФЗ).

**Критерий готовности**: реквизиты собираются и шифруются; верификация
работает в режиме `manual`; `pytest` зелёный.

### B9 — Переговоры и приём оплаты ✅ done

1. `src/comms/payment_policy.py` — режимы `card_required` (настаивать на
   карте/СБП) и `card_preferred` (уступать сделке площадки); схемы
   `advance` / `partial_advance` / `on_delivery`.
2. `src/comms/negotiation.py` — скрипт заключения заказа: условия →
   реквизиты → ответ на возражение про «безопасную сделку».
3. `src/workflow/state_machine.py` — состояния `NEGOTIATING`,
   `AWAITING_PAYMENT`, `PAID`.

**Критерий готовности**: переговоры следуют политике; состояния оплаты
в машине состояний; `pytest` зелёный.

### B2 — Почта

`provisioning/email.py` — создание ящиков + чтение кодов/ссылок
(IMAP/SMTP или temp-mail API); модель `Mailbox`.

### B3 — Телефон

`provisioning/phone.py` — SMS-активация (sms-activate / 5sim / onlinesim):
взять номер → ждать SMS → код; модель `PhoneNumber`.

### B4 — Капча

`provisioning/captcha.py` — решение капчи (RuCaptcha/2Captcha/anti-captcha)
+ локальный fallback; модель `CaptchaRequest`.

### B5 — Оркестратор регистрации

`provisioning/registrar.py` + `identity.py` — Playwright-регистрация на
площадке: форма → капча → подтверждение почтой/SMS → vault; bounded-повторы;
модель `ProvisioningJob`.

### B6 — Хранилище учётных записей

`src/security/vault.py` (расширение) — зашифрованные пароли + storage_state;
аудит доступа (`AuditLog`).

### B7 — Агент-планировщик

`agent/planner.py`, `agent/commander.py` — NL-команда → декомпозиция в
задания; оценка стоимости; проверка бюджета и баланса.

### B8 — Аудит безопасности и юридическая проверка

Review всех интеграций (провайдеры SMS/капчи, банк), обновление матрицы
рисков и стоп-листа в `docs/SCOPE.md`.

---

## Правила ведения прогресса

- После завершения каждого этапа `progress.json` обновляется: `pending` → `done`.
- Каждый этап коммитится в git с осмысленным сообщением (`feat(db): ...`).
- Порядок строгий: этап N начинается только после критерия готовности этапа N−1.
- Запрещено в автоматическом режиме: регистрации, накрутка, отзывы на
  маркетплейсах, передача/ввод персональных данных, платёжные операции.
  Полный стоп-лист — в `docs/SCOPE.md`.


