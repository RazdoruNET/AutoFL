# ==============================================================
# AutoFL — конфигурация
# Скопируйте в .env и заполните:  cp .env.example .env
# ==============================================================

# ===================== LLM ====================================
# Провайдер: ollama (локально) | openai | anthropic
LLM_PROVIDER=ollama
# Адрес локального инференса (Ollama)
OLLAMA_BASE_URL=http://127.0.0.1:11434
# Целевая модель
LLM_MODEL=nemotron-3-nano:4b
# Ключи внешних провайдеров (пусто — не используются)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# Таймаут запроса генерации, секунд
LLM_TIMEOUT_SECONDS=120
# Число ретраев при сбоях
LLM_RETRIES=2
# Температура генерации (классификация — ниже, генерация текста — выше)
LLM_TEMPERATURE=0.3

# ===================== Площадки ===============================
# Через запятую: kwork,flru,youdo
PLATFORMS=
# Браузер
BROWSER_HEADLESS=true
BROWSER_SLOW_MO_MS=0
# Прокси (пусто — прямое соединение). Для площадок используйте резидентные.
PROXY_URL=
# Пустой — использовать системный (или эмулированный) fingerprint
USER_AGENT=
# Пауза между запросами к ленте, секунд
DISCOVER_RATE_LIMIT_SECONDS=30

# ===================== Поведение ==============================
# true — полная автономность (включается только после Этапа 8)
AUTO_EXECUTE=false
# true — любое внешнее действие (отклик/отправка/сообщение) требует аппрува
APPROVAL_REQUIRED=true
# Порог допуска задачи по безопасности 0.0..1.0
MIN_SAFETY_SCORE=0.85
# Параллельных задач
MAX_CONCURRENT_TASKS=1
# Дневной лимит расходов (LLM+прокси), рублей; 0 — без лимита
DAILY_BUDGET_RUB=0
# Максимум итераций ревизий по одному заданию
MAX_REVISIONS=2
# true — ничего не отправлять и не отвечать, только логировать
DRY_RUN=true

# ===================== Регистрация (Трек B) ==================
# Провайдеры (по умолчанию dry_run — эмуляция без внешних сервисов):
#   EMAIL_PROVIDER   : dry_run | 1secmail (бесплатная temp-почта)
#   SMS_PROVIDER     : dry_run | sms_activate | 5sim
#   CAPTCHA_PROVIDER : dry_run | rucaptcha | 2captcha
EMAIL_PROVIDER=dry_run
SMS_PROVIDER=dry_run
CAPTCHA_PROVIDER=dry_run
# Ключи реальных провайдеров (оставить пустыми для dry_run)
SMS_ACTIVATE_API_KEY=
FIVE_SIM_API_KEY=
CAPTCHA_API_KEY=
# Сервис капчи: rucaptcha | 2captcha
CAPTCHA_SERVICE=rucaptcha
# Коды сервисов/продуктов для SMS-активации (JSON):
#   {"kwork": "kwork", "flru": "fl", "youdo": "youdo"}
SMS_SERVICE_MAP={}

# ===================== Финансы и оплата (Трек B) =============
# Режим приёма оплаты:
#   card_required — настаивать на оплате напрямую на карту/СБП
#   card_preferred — предлагать карту, при отказе — сделка площадки
PAYMENT_POLICY=card_required
# Схема оплаты: advance | partial_advance | on_delivery
PAYMENT_SCHEME=advance
# Верификация прихода оплаты: manual (оператор) | bank_sms
PAYMENT_VERIFICATION_MODE=manual
# Реквизиты по умолчанию для приёма оплаты
DEFAULT_CARD_HOLDER=
DEFAULT_CARD_SBP_PHONE=
DEFAULT_CARD_BANK=
# Авто-трата без подтверждения оператора, руб. (выше — аппрув)
SPENDING_AUTO_LIMIT_RUB=200
# Ключ шифрования vault (пусто — автогенерация в data/vault.key)
VAULT_KEY=

# ===================== Telegram (админ) =======================
# Токен админ-бота (BotFather) — мониторинг и аппрувы
TELEGRAM_BOT_TOKEN=
# id чата оператора (число)
ADMIN_CHAT_ID=

# ===================== База данных =============================
DB_URL=sqlite+aiosqlite:///data/autofl.db

# ===================== HTTP API ================================
API_HOST=127.0.0.1
API_PORT=8000

# ===================== Логи ====================================
LOG_LEVEL=INFO
