# Эксплуатация AutoFL (RUNBOOK)

## Запуск и остановка

```bash
python -m src.main            # проверка окружения: конфиг + логи
python -m src.db.init_db      # создать схему БД (если нет data/autofl.db)
uvicorn src.api.app:app       # API
```

Остановка — `Ctrl+C` / SIGTERM (graceful shutdown).

## БД

- Файл по умолчанию: `data/autofl.db` (`DB_URL` в `.env`).
- Каталог `data/` создаётся автоматически при инициализации движка.
- Бэкап: копировать файл БД при остановленном процессе (SQLite — один файл).
- Миграции: пока `create_all` (`init_db`). На Этапе 2 — Alembic.

## Vault (шифрование)

- Ключ: `VAULT_KEY` в `.env` **или** автогенерация в `data/vault.key`.
- Если ключ потерян/изменён — старые шифрованные данные (карты, пароли,
  коды) не расшифруются. Храните `VAULT_KEY` в надёжном месте.
- В логах никогда не должно появляться: полных PAN, паролей, кодов.

## Провайдеры регистрации

| Симптом | Причина / действие |
|---|---|
| `NO_BALANCE` (sms-activate) | пополнить баланс сервиса |
| `NO_NUMBERS` | нет номеров под сервис — скорректировать `SMS_SERVICE_MAP` |
| `BAD_KEY`, `401` | неверный ключ — проверить `.env` |
| `Неизвестный режим/провайдер` | неверное значение `*_PROVIDER` в `.env` |
| Live-ошибка клиента | клиенты не проверены вживую — сверить параметры с документацией сервиса |

Коды сервисов для площадок (`SMS_SERVICE_MAP`) сверяются с документацией
sms-activate/5sim после получения ключа.

## Безопасность и стоп-лист

- Стоп-правила (`src/brain/safety.py`) срабатывают до LLM; полный стоп-лист —
  в `docs/SCOPE.md` (§ 4).
- Автономность: до Этапа 8 `AUTO_EXECUTE=false`, `APPROVAL_REQUIRED=true`.
- Приём оплаты на карту: режимы `card_required` / `card_preferred`
  (`PAYMENT_POLICY`). `card_required` = обход сделки площадки — риск бана
  (см. SCOPE, матрица рисков).

## Фазы развёртывания (Этап 11)

| Фаза | Что включено |
|---|---|
| A (read-only) | поиск + классификация + отчёт; без мутаций |
| B (draft-only) | LLM готовит черновики; отправляет оператор |
| C (supervised) | авто-отправка низкорисковых с аппрувами и лимитами |
| D (semi-auto) | автономный цикл с лимитами, kill switch, ревизией |

## Логи

- `logs/autofl.log` (rotating: 5 МБ × 3).
- Уровень — `LOG_LEVEL` (по умолчанию INFO).

## Типичные операции

```bash
# пересоздать схему БД (осторожно: сотрёт данные)
rm data/autofl.db && python -m src.db.init_db

# посмотреть созданные аккаунты (dry-run демо)
sqlite3 data/autofl.db "SELECT id, login, status FROM accounts;"

# прогнать тесты
pytest -v
```
