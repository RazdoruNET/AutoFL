# Команды оператора (Трек B7)

## Назначение

Оператор отдаёт системе естественно-языковую команду — агент сам планирует
и выполняет регистрацию аккаунтов на фриланс-площадках.

## Поддерживаемые команды

```
заведи N аккаунтов на <площадка>
заведите N аккаунтов для <площадка>
```

Площадки (из реестра адаптеров): `kwork`, `flru`, `youdo`.
Число `N` — от 1 до 20.

Примеры:

```
заведи 3 аккаунта на kwork
заведите 2 аккаунта для flru
```

## Разбор и исполнение

`AgentPlanner` (`src/agent/planner.py`):

1. `plan(command)` — регулярное выражение разбирает команду; валидирует
   площадку (должна быть в реестре) и число аккаунтов; возвращает список
   заданий.
2. `execute(command)` — для каждого задания вызывает
   `AccountRegistrar.register(platform)`.

Полный конвейер одного аккаунта:

```
профиль → почта (ящик + код) → SMS-номер (+ код) → капча →
шифрование учётных данных (vault) → запись в БД:
  accounts, mailboxes, phone_numbers, provisioning_jobs
```

## Режимы работы

| Режим | Настройка | Что происходит |
|---|---|---|
| `dry_run` (по умолчанию) | `EMAIL/SMS/CAPTCHA_PROVIDER=dry_run` | эмуляция: ящик `@dry.local`, коды `111111/222222`, аккаунт создаётся в БД |
| реальные провайдеры | `SMS_PROVIDER=sms_activate` и т.п. + ключи | реальная почта/номер/капча через API |

Переключение и ключи — в [SETUP.md](SETUP.md) (раздел «Регистрация»).

## Демо

```bash
python demo_register.py
```

Выполняет команду `заведи 2 аккаунта на kwork` и печатает план и результаты
(в dry_run). Аккаунты сохраняются в `data/autofl.db`.

## Ограничения и безопасность

- Валидация: неизвестная площадка или число вне 1–20 → `ValueError` с пояснением.
- Реальная регистрация требует: провайдеров с ключами, адаптера формы площадки
  (Playwright) и — важное — **согласия с ToS площадки и рисками**
  (`docs/SCOPE.md`). До Этапа 8 система не выполняет автономных внешних
  действий без одобрения оператора.

## Планы (Этап B7)

- `AgentCommander` (`src/agent/commander.py`) — приём команд через
  Telegram-админа; интеграция с `ApprovalGate` и `SpendingController`
  (оценка стоимости регистрации, проверка бюджета).
