# Скоуп и политики AutoFL (Этап 0)

> Домен системы, таксономия заданий, стоп-лист и матрица рисков.
> Этот документ — источник правды для правил безопасности
> (`src/brain/safety.py`).

## 1. Миссия

Автономно искать, вести и выполнять **простые и безопасные** задания на
фриланс-площадках: от обнаружения задания до отправки результата и
сопровождения заказчика. Автономность включается поэтапно (фазы A–D)
и до этапа 8 всегда предполагает контроль оператора.

## 2. Целевые площадки (адаптеры)

- Kwork — `kwork`
- FL.ru — `flru`
- YouDo — `youdo`

Адаптерная архитектура (`src/platforms/`) позволяет добавлять площадки
без изменения ядра. Публичных API у площадок нет — используется Playwright.

## 3. Исполняемые типы заданий

| Категория | Примеры | Движок |
|---|---|---|
| `text` | рерайт, перевод, описание товара, SEO-текст | `TextExecutor` (LLM) |
| `data` | парсинг, преобразование форматов, таблицы, дедупликация | `DataExecutor` |
| `code` | маленькие скрипты, преобразование данных | `CodeExecutor` + песочница |

Задание считается «простым», если классификатор относит его к одной из
категорий выше, сложность `simple|medium` и `safety_score >= MIN_SAFETY_SCORE`.

## 3.1 Платёжная модель (Трек B)

Агент **не использует внутренний счёт площадки** для получения оплаты.
Платежи принимаются напрямую на выделенную **дебетовую карту / СБП-номер**
агента (`FundingSource`):

- `PAYMENT_POLICY=card_required` — настаивать на оплате напрямую на карту/СБП
  при заключении заказа (повышенный риск бана за обход сделки площадки);
- `PAYMENT_POLICY=card_preferred` — предлагать карту, при отказе клиента
  соглашаться на безопасную сделку площадки;
- схемы оплаты: `advance` (100% предоплата), `partial_advance` (частичная),
  `on_delivery` (по готовности);
- приход подтверждается верификатором (`manual` — оператор, `bank_sms` — сверка
  суммы) и фиксируется в `Transaction` (идемпотентно по `ref`).
- траты на инфраструктуру (SMS-активация, капча, почта, подписки) проходят
  через `SpendingController` (лимиты категорий + порог авто-трат).

## 4. Стоп-лист (никогда в автоматическом режиме)

- регистрации на сайтах и в аккаунтах;
- накрутка, «оставь отзыв на маркетплейсе», голосования, прохождение капчи;
- ввод/передача персональных или платёжных данных (паспорт, карты, CVV, SMS-коды);
- работа с чужими аккаунтами, обход защит, «взлом»;
- любые операции с деньгами заказчика, переводы;
- задания «перейди по ссылке», «проверь ссылку» без доверия к отправителю.

Задание из стоп-листа отклоняется **детерминированными правилами до LLM**
(`src/brain/safety.py`), модель не участвует в решении.

## 5. Матрица рисков и митигации

| Риск | Вероятность | Митигация |
|---|---|---|
| Бан аккаунта (нарушение ToS) | высокая | read-only фазы, вежливый rate-limit, человеческие задержки, резидентные прокси, ручной вход, супервизный режим |
| Обход сделки площадки (оплата «на карту») | высокая | аккуратные формулировки, режим `card_preferred` как безопасный дефолт, понимание риска бана |
| 115-ФЗ / мониторинг банка | средняя | отдельная карта, лимиты, ручная верификация прихода, сверка транзакций |
| Налоги при приёме оплаты | всегда | учёт дохода, самозанятость/ИП, налоговое декларирование |
| Капча / 2FA | средняя | только ручное решение оператором + сохранение сессии |
| Скам-задания | средняя | детерминированные правила + LLM-скоринг + порог |
| Низкое качество результата | средняя | `QualityChecker` + ручная ревизия первых N задач |
| Цикл ревизий | средняя | bounded (`MAX_REVISIONS`), дальше — эскалация |
| Краш / двойная отправка | средняя | state machine + идемпотентные переходы |
| Расходы > доходов | средняя | `DAILY_BUDGET_RUB`, `MAX_CONCURRENT_TASKS`, метрики |
| Вывод денег / KYC | всегда | только вручную оператором, вне скоупа бота |

## 6. Уровни автономии (фазы развёртывания)

- **Фаза A (read-only)**: поиск + классификация + отчёт оператору. Мутаций нет.
- **Фаза B (draft-only)**: LLM готовит черновики; отправляет оператор вручную.
- **Фаза C (supervised)**: авто-отправка низкорисковых заданий с аппрувами
  и дневными лимитами.
- **Фаза D (semi-auto)**: автономный цикл с лимитами, kill switch
  и ежедневной ревизией.

Управление: флаги `.env` — `AUTO_EXECUTE`, `APPROVAL_REQUIRED`, `DRY_RUN`.

## 7. Юридический дисклеймер

Инструмент предназначен для изучения автоматизации и контролируемого ведения
личного аккаунта **в соответствии с правилами площадок**. Пользователь несёт
полную ответственность за применение системы. Приоритет — соблюдение ToS,
законов и этики.

Приём оплаты напрямую на карту/СБП: это обход защищённой сделки площадки
(нарушение ToS, риск бана) и, в РФ, влечёт обязанность налогового учёта
доходов (самозанятость/ИП) и внимание банковского мониторинга (115-ФЗ).
Владелец карты отвечает за эти последствия лично.
